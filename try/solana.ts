import { deriveSolanaKey } from "../src/utils/createAccount.ts";

const mnemonic =
  "put story margin fiber expand animal ring hole beef extra grape rate";

console.log(deriveSolanaKey(mnemonic));
// console.log(deriveSolanaKey(mnemonic));
